{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "dotnet publish ./MovieApi/MovieApi.csproj --no-restore -c Release -o out"
      ]
    },
    "run": {
      "commands": [
        "dotnet out/MovieApi.dll"
      ]
    }
  }
}